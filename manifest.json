{
  "manifest_version": 3,
  "name": "Telegram Vocabulary Saver",
  "description": "Highlight a word, add its meaning, and send it to a Telegram group.",
  "version": "1.0.0",
  "permissions": [
    "storage",
    "scripting",
    "activeTab"
  ],
  "permission_descriptions": {
    "storage": "Stores the Telegram bot token and chat ID locally via chrome.storage.sync so the extension can send messages without prompting every time.",
    "scripting": "Injects the inline popup scripts and styles into the active tab when you select text, enabling the save workflow.",
    "activeTab": "Accesses the currently active tab only after the user interacts with the extension, allowing it to read the selected word and show the popup."
  },
  "host_permissions": [
    "https://api.telegram.org/*"
  ],
  "host_permissions_descriptions": {
    "https://api.telegram.org/*": "Sends the formatted vocabulary entry to Telegram's Bot API using the credentials you provide."
  },
  "background": {
    "service_worker": "background.js"
  },
  "action": {
    "default_title": "Telegram Vocabulary Saver",
    "default_popup": "popup.html"
  },
  "content_scripts": [
    {
      "matches": ["<all_urls>"],
      "js": ["content.js"],
      "css": ["styles/popup.css"]
    }
  ],
  "options_page": "options.html"
}
